{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel dashboard.html",
    "build": "parcel build dashboard.html"
  },
  "dependencies": {
    "axios": "^0.21.1"
  }
}
